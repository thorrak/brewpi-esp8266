; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter, extra scripting
;   Upload options: custom port, speed and extra flags
;   Library options: dependencies, extra library storages
;
; Please visit documentation for the other options and examples
; http://docs.platformio.org/en/stable/projectconf.html

[platformio]
; Added this here to "hard code" the environment for macro debugging/visuals
;default_envs = esp32_wifi_tft


[env]
lib_compat_mode = off
; Builds the Vue UI from ui/ and copies artifacts into data/ when the
; `buildfs` (or `uploadfs`) target runs. Firmware-only builds are unaffected.
; Requires Node.js/npm on PATH — see ui/README.md.
extra_scripts = pre:scripts/build_ui.py


[common]
framework = espidf
; The default platform & platform package only applies to ESP32 derivatives
platform = espressif32@6.13.0
lib_deps =
    bblanchon/ArduinoJson @ ^7.4.2
    https://github.com/thorrak/thorlog.git
    ;https://github.com/thorrak/esp32_ds18b20.git ; todo - replace this with the idf components directly
    #thorrak/esp_wifi_config @ 0.0.2
    #thorrak/esp_bus @ ^1.0.3
lib_deps_graphics =
    lovyan03/LovyanGFX@^1.2.19
; Let's also include some other common build flags for ease-of-use
build_flags =
    !python3 scripts/gen_version.py
    -DCONFIG_USE_NATIVE_CPP_NEW
    -D THORLOG_LOG_LEVEL=0              ; Set Serial log level (6 = high)
    -D CONFIG_NIMBLE_CPP_LOG_LEVEL=0    ; Set NimBLE log level (4 = high)
    -D DISABLE_LOGGING                  ; This will remove log lib from sketch
    -D CONFIG_WIFI_CFG_WEBUI_CUSTOM_PATH=\"/littlefs/wifiui\"
    -D CONFIG_WIFI_CFG_ENABLE_WEBUI=1
    -D FIRMWARE_REVISION=\"v17-alpha2\"

upload_speed = 460800
monitor_speed = 115200
; These are true for most devices on MacOS - Will likely need to be changed if compiling on other platforms
build_type = release
monitor_dtr = 1
monitor_rts = 1


[env:esp32_wifi_tft]
board = lolin_d32
platform = ${common.platform}
framework = ${common.framework}
; board_build.arduino.upstream_packages = no
; For esp32_wifi_tft we want to enable LCD support, TFT support, and WiFi support
build_flags =
    ${common.build_flags}
    -DBREWPI_LCD
    ; BREWPI_TFT is the flag we're using for TFTs in general
    ; BREWPI_TFT_ILI9341 is the flag we're using for the "big" (LoLin) TFTs
    -DBREWPI_TFT
    -DBREWPI_TFT_ILI9341
    -DD32_PRO_TFT
    -DCONNECT_VIA_WIFI
    -DHAS_BLUETOOTH
    -DEXTERN_SENSOR_ACTUATOR_SUPPORT
    ; -DENABLE_PROMETHEUS_SERVER
    -DENABLE_HTTP_INTERFACE
    -DESP32_STOCK
lib_deps =
    ${common.lib_deps}
    ${common.lib_deps_graphics}
upload_speed = ${common.upload_speed}
monitor_speed = ${common.monitor_speed}
board_build.partitions = 4mb_no_ota.csv
board_build.filesystem = littlefs
build_type = ${common.build_type}
monitor_filters =
    esp32_exception_decoder


[env:esp32_wifi_iic]
board = lolin_d32
platform = ${common.platform}
framework = ${common.framework}
; For esp32_wifi_iic we want to enable LCD support, IIC support, and WiFi support
build_flags =
    ${common.build_flags}
    -DBREWPI_LCD
    -DBREWPI_IIC
    -DCONNECT_VIA_WIFI
    -DHAS_BLUETOOTH
    -DEXTERN_SENSOR_ACTUATOR_SUPPORT
    ; -DENABLE_PROMETHEUS_SERVER
    -DENABLE_HTTP_INTERFACE
    -DESP32_STOCK
lib_deps =
    ${common.lib_deps}
upload_speed = ${common.upload_speed}
monitor_speed = ${common.monitor_speed}
board_build.partitions = 4mb_no_ota.csv
board_build.filesystem = littlefs
build_type = ${common.build_type}
monitor_filters =
    esp32_exception_decoder


; [env:esp32_c3_wifi]
; platform = espressif32
; board = lolin_c3_mini
; framework = ${common.framework}
; ; For esp32_c3_wifi we want to enable LCD support, TFT support, and WiFi support
; build_flags =
;     ${common.build_flags}
;     -DBREWPI_LCD
;     -DBREWPI_IIC
;     -DCONNECT_VIA_WIFI
;     -DEXTERN_SENSOR_ACTUATOR_SUPPORT
;     ; -DENABLE_PROMETHEUS_SERVER
;     ; -DENABLE_HTTP_INTERFACE
;     -DESP32C3
; lib_deps =
;     ${common.lib_deps}
; upload_speed = ${common.upload_speed}
; monitor_speed = ${common.monitor_speed}
; board_build.embed_txtfiles = ${common.board_build.embed_txtfiles}
; board_build.partitions = 4mb_inc_ota.csv
; ;board_build.filesystem = littlefs
; build_type = ${common.build_type}
; monitor_filters =
;     esp32_exception_decoder


[env:esp32_s2_wifi]
board = lolin_s2_mini
platform = ${common.platform}
framework = ${common.framework}
; v4.4.4 of esp-idf introduces a bug in the IIC implementation that causes MASSIVE delays
; in the IIC bus on the S2 (meaning that writes to the LCD grind the controller to a halt). 
; This appears to be related to https://github.com/espressif/arduino-esp32/issues/8480#issuecomment-1708909457
; For esp32_s2_wifi we want to enable LCD support, IIC support, and WiFi support
; There is no Bluetooth support on this chip. 
build_flags =
    ${common.build_flags}
    -DBREWPI_LCD
    -DBREWPI_IIC
    -DCONNECT_VIA_WIFI
    -DENABLE_HTTP_INTERFACE
    -DEXTERN_SENSOR_ACTUATOR_SUPPORT
    ; -DENABLE_PROMETHEUS_SERVER
    -DESP32S2
lib_deps =
    ${common.lib_deps}
upload_speed = ${common.upload_speed}
monitor_speed = ${common.monitor_speed}
board_build.partitions = 4mb_inc_ota.csv
board_build.filesystem = littlefs
build_type = ${common.build_type}
monitor_filters =
    esp32_exception_decoder


; [env:esp32_s2_serial]
; board = lolin_s2_mini
; platform = ${common.platform}
; framework = ${common.framework}
; ; v4.4.4 of esp-idf introduces a bug in the IIC implementation that causes MASSIVE delays
; ; in the IIC bus on the S2 (meaning that writes to the LCD grind the controller to a halt). 
; ; This appears to be related to https://github.com/espressif/arduino-esp32/issues/8480#issuecomment-1708909457
; ; For esp32_s2_serial we want to enable LCD support, IIC support, and Serial support
; ; There is no Bluetooth support on this chip. 
; build_flags =
;     ${common.build_flags}
;     -DBREWPI_LCD
;     -DBREWPI_IIC
;     -DCONNECT_VIA_SERIAL
;     ; -DEXTERN_SENSOR_ACTUATOR_SUPPORT
;     -DESP32S2
; lib_deps =
;     ${common.lib_deps}
; upload_speed = ${common.upload_speed}
; monitor_speed = ${common.monitor_speed}
; board_build.embed_txtfiles = ${common.board_build.embed_txtfiles}
; board_build.partitions = 4mb_inc_ota.csv
; board_build.filesystem = littlefs
; build_type = ${common.build_type}
; custom_component_remove = ${common.custom_component_remove}
; monitor_filters =
;     esp32_exception_decoder

[env:esp32_wifi_espi]
board = esp32dev
platform = ${common.platform}
framework = ${common.framework}
; board_build.arduino.upstream_packages = no
; For esp32_wifi_tft we want to enable LCD support, TFT support, and WiFi support
build_flags =
    ${common.build_flags}
    -DBREWPI_LCD
    -DBREWPI_TFT
    -DBREWPI_TFT_ESPI
    -DHAS_AXP192
    -DCONNECT_VIA_WIFI
    -DHAS_BLUETOOTH
    -DEXTERN_SENSOR_ACTUATOR_SUPPORT
    ; -DENABLE_PROMETHEUS_SERVER
    -DENABLE_HTTP_INTERFACE
    -DESP32_STOCK
    -DoneWirePin=33       ; GPIO 13 conflicts with ST7789 SPI SCLK on M5StickC Plus

lib_deps =
    ${common.lib_deps}
    ${common.lib_deps_graphics}
upload_speed = 1500000
monitor_speed = ${common.monitor_speed}
board_build.partitions = 4mb_no_ota.csv
board_build.filesystem = littlefs
build_type = ${common.build_type}
monitor_filters =
    esp32_exception_decoder
